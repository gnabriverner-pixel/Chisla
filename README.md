# Chisla — HISTORICAL PROJECT-META / DONOR

This repository is an earlier AI Studio project and project-meta workspace for the broader Digital Code / Numbers work.

## Current source-of-truth boundary

It is **not** the current operational control center and **not** a production runtime repository.

Canonical current sources:

- `gnabriverner-pixel/digital-code-system` — Telegram / Deep / PDF / Mini App production core.
- `gnabriverner-pixel/Zerkalo` — public site / trust / privacy layer.
- Notion `00 — DIGITAL CODE AGENT CONTROL CENTER — READ FIRST` — current project memory and agent handoff layer.

## Why this repository is kept

The history contains potentially useful project-meta and agent-operating documents. Treat it as historical/reference material until unique content has been reviewed and, where still valuable, migrated into the current Notion knowledge system.

## Agent rule

Do not:

- infer current release status from this repository;
- use its older plans to override `digital-code-system/STATUS.md`;
- treat the AI Studio app as the current Digital Code product surface.

Do:

- use it only when looking for historical provenance or unique project-meta material;
- prefer the current canonical repositories and Notion control center for active work.

The previous generic AI Studio README was removed because it obscured the repository's real role.
